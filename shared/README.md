# Shared Types

This directory contains TypeScript types and interfaces shared between the backend and frontend.

Currently includes:
- `types.ts` - Core data models (Artist, Artwork, etc.)

You can import these in both backend and frontend for type consistency.

