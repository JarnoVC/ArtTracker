name: Daily artist update

on:
  schedule:
    - cron: "0 7 * * *"
  workflow_dispatch:

jobs:
  call-scrape:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render scrape
        run: |
          curl -X POST https://arttracker-backend.onrender.com/api/cron/sync
